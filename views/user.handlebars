<!-- this page shows users name along witht the devices they have liked-->
<P>{{message}}</P>
<section class="addPadding">
	<h1>{{title}}</h1>
	<hr>
	<h2>{{username}}</h2>
	<hr>
	<h3> My devices! </h3>
	{{#each devices}}
		<h4>{{this}}</h4> 
	{{else}}
		<h4> No devices </h4>
	{{/each}}
</section>